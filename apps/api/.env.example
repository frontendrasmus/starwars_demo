# Copy to .env and fill in your keys.
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# Optional: override the default port (Hono listens on 8080).
# PORT=8080

# Optional: allowed CORS origin (defaults to the Vite dev origin).
# CORS_ORIGIN=http://localhost:5173
